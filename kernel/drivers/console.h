#ifndef _CONSOLE_H_
#define _CONSOLE_H_

/**
 * Print the string to the console window. This uses the same sort of format
 * parsing that printf uses. Returns the amount of characters printed to
 * the screen.
 */
int cprintf(char* fmt, ...);

#endif
